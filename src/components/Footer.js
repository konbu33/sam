import React from 'react';
import styled from 'styled-components';

const SFooter = styled.main`
	outline: 1px dotted gray;
`;

const Footer = () => (
	<SFooter>
		<h1>Footer</h1>
	</SFooter>
);

export default Footer;

